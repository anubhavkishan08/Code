#include<bits/stdc++.h>
using namespace std;
int main()
{
    string s,s1,s2;
    int flag=0;
    cin>>s>>s1;
    for(int i=0;i<s.size();i++)
    {
        for(int j=1;j<26;j++)
        {
            if(s[i]+j==s2[i])
                   continue;
        }
    }
      for(int i=0;i<s2.size();i++)
             cout<<s2.size();
          //else
            //cout<<"NO";
}
